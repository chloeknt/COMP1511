#include <stdio.h>

int main(void) {

    
    // some basic maths
    int x = 5;
    int y = 10;
    int result;
    result = (x+y) * x;
    printf("My maths comes out to: %d\n", result);
    
    
return 0; 
    
}
