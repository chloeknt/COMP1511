#include <stdio.h> 

int main (void) { 

    int num; 
    printf("Enter number: "); 
    scanf("%d", &num); 

    // printing out a vertical line of asterisks
    /* int i = 1; 
    while (i <= num) {
        printf("*\n");
        i++;
    } */
    
    //printing out a n x n box of asterisks
    /* int i = 1; 
    while (i <= num) {
        int j = 1;
        while (j <= num) {
        printf("*");
        j++;
        }
    printf("\n");
    i++;
    } */
    
    //printing out a diagonal of asterisks
    /* int i = 1; 
    while (i <= num) {
        int j = 1;
        while (j <= num) {
            if (i == j) {
                printf("*");
            } else { 
                printf("-");
            }
            j++;
        }
    printf("\n");
    i++;
    } */
    
    //printing out a triangle of asterisks 
    /* int i = 1; 
    while (i <= num) {
        int j = 1;
        while (j <= num) {
            if (i == num || j == num || i + j > num) {
                printf("*");
            } else { 
                printf("-");
            }
            j++;
        }
    printf("\n");
    i++;
    } */
    
    //printing an alternate pattern of asterisks 
    int i = 1; 
    while (i <= num) {
        int j = 1;
        while (j <= num) {
            if (j % 2 == 0) {
                printf("*");
            } else { 
                printf("-");
            }
            j++;
        }
    printf("\n");
    i++;
    }
    
    return 0;  
} 

